# 🖥️ ЗАПУСК БОТА НА ЛОКАЛЬНОМ КОМПЬЮТЕРЕ

**Автоматический запуск при включении ПК + Фоновая работа + Ежечасное обновление данных**

---

## 📋 ШАГ 1: Подготовка файлов

### Что скачать:

1. **Весь репозиторий** (или минимальный набор):
   ```
   scripts/telegram/working_bot.py
   scripts/data_collection/improved_collector.py
   scripts/analytics/analytics_reports.py
   requirements.txt
   process_period_data.py
   data/fiksa_database.db
   config/  (папка с Google Sheets config)
   ```

2. **Структура на локальном ПК:**
   ```
   C:\QAyta-Bot\
   ├── scripts/
   │   ├── telegram/
   │   │   └── working_bot.py
   │   ├── data_collection/
   │   │   └── improved_collector.py
   │   └── analytics/
   │       └── analytics_reports.py
   ├── config/               (Google Sheets config)
   ├── data/
   │   └── fiksa_database.db
   ├── 123/                  (папка для загруженных файлов)
   ├── reports/              (папка для отчетов)
   ├── requirements.txt
   ├── process_period_data.py
   └── ЗАПУСК_БОТ.bat        (автозапуск)
   ```

---

## 🔧 ШАГ 2: Установка зависимостей

### Откройте PowerShell или CMD как администратор:

```batch
cd C:\QAyta-Bot

# Установите Python зависимости
pip install -r requirements.txt
```

**Требуемые пакеты:**
- python-telegram-bot==21.0
- pandas
- openpyxl
- requests
- google-auth-oauthlib
- google-auth-httplib2
- google-api-python-client

---

## 🚀 ШАГ 3: Автозапуск при включении ПК

### Вариант A: Через Task Scheduler (РЕКОМЕНДУЕТСЯ)

**1. Создайте скрипт `ЗАПУСК_БОТ.bat`:**

```batch
@echo off
setlocal enabledelayedexpansion

REM Запуск Telegram бота в фоне
cd C:\QAyta-Bot
start "" pythonw.exe scripts/telegram/working_bot.py

REM Запуск ежечасного обновления данных
start "" pythonw.exe ОБНОВЛЕНИЕ_КАЖДЫЙ_ЧАС.py

exit /b 0
```

**2. Создайте скрипт `ОБНОВЛЕНИЕ_КАЖДЫЙ_ЧАС.py`:**

```python
#!/usr/bin/env python3
"""
Ежечасное обновление данных из Google Sheets
Запускается автоматически каждый час
"""
import time
import subprocess
import sys
from pathlib import Path
from datetime import datetime

BASE_DIR = Path(__file__).resolve().parent
COLLECTOR_SCRIPT = BASE_DIR / 'scripts' / 'data_collection' / 'improved_collector.py'

def update_data():
    """Обновить данные из Google Sheets"""
    print(f"[{datetime.now()}] Начинаю обновление данных...")
    try:
        result = subprocess.run(
            [sys.executable, str(COLLECTOR_SCRIPT)],
            capture_output=True,
            text=True,
            timeout=600
        )
        if result.returncode == 0:
            print(f"[{datetime.now()}] ✅ Данные обновлены успешно")
        else:
            print(f"[{datetime.now()}] ❌ Ошибка обновления: {result.stderr}")
    except Exception as e:
        print(f"[{datetime.now()}] ❌ Ошибка: {e}")

def main():
    """Основной цикл - обновление каждый час"""
    print("🟢 Сервис обновления запущен - будет работать каждый час")
    print("   Даже если компьютер в режиме сна, обновится при пробуждении")
    
    while True:
        try:
            update_data()
            # Ждем 1 час (3600 секунд)
            time.sleep(3600)
        except KeyboardInterrupt:
            print("\n⛔ Сервис остановлен")
            break
        except Exception as e:
            print(f"❌ Ошибка: {e}")
            time.sleep(60)  # Попробуем снова через минуту

if __name__ == '__main__':
    main()
```

**3. Откройте Task Scheduler:**

- Нажмите `Win + R`
- Введите `taskschd.msc` и нажмите Enter

**4. Создайте новую задачу:**

- `Действие` → `Создать простую задачу`
- **Имя:** `QAyta Bot`
- **Описание:** `Автоматический запуск Telegram бота`
- **Триггер:** `При запуске системы`
- **Действие:** 
  - Программа: `C:\Windows\System32\cmd.exe`
  - Аргументы: `/c C:\QAyta-Bot\ЗАПУСК_БОТ.bat`
- **Нажмите:** `Далее` → `Готово`

**5. Отредактируйте задачу:**

- Дважды кликните на `QAyta Bot`
- Вкладка `Условия`:
  - ✅ `Включить компьютер только при необходимости`
  - ✅ `Запускать задачу как можно скорее после пропуска`
- Вкладка `Параметры`:
  - ✅ `Запускать с наивысшими правами`
  - `Время ожидания переответа заданий`: `5 минут`

---

## 🔄 ШАГ 4: Ежечасное обновление Google Sheets

Скрипт `ОБНОВЛЕНИЕ_КАЖДЫЙ_ЧАС.py` уже в батнике сверху.

**Что происходит каждый час:**
1. ✅ Импорт новых данных из Google Sheets
2. ✅ Обновление существующих записей
3. ✅ Обновление локальной БД
4. ✅ Даже если ПК спит - запустится при пробуждении

---

## 🎯 ШАГ 5: Тестирование

### Проверьте, что бот работает:

**В Telegram:**
1. Найдите бота `@QAytaBot` (или ваше имя)
2. Отправьте `/start`
3. Проверьте команды:
   - `/stats` - статистика БД
   - `/update` - обновить данные
   - Загрузите файл - должен сохраниться в `123/`

**На компьютере:**
1. Откройте папку `C:\QAyta-Bot\123\` - должны появиться загруженные файлы
2. Откройте папку `reports/` - должны быть сгенерированные отчеты
3. Проверьте папку `logs/` - должны быть логи работы

---

## 📊 ШАГ 6: Мониторинг и проверка

### Как проверить что всё работает:

**Проверка бота:**
```
Telegram → /stats
```

**Проверка обновления данных (каждый час):**
```
В логах должна появиться строка: "[время] Начинаю обновление данных..."
```

**Проверка логов:**
```
C:\QAyta-Bot\logs\telegram_bot.log
C:\QAyta-Bot\logs\update_hourly.log
```

---

## ⚠️ ВАЖНО: Отключение спящего режима

Чтобы бот работал даже когда монитор спит:

**Способ 1: Через параметры питания**
1. `Пуск` → `Параметры`
2. `Система` → `Питание и состояние`
3. `Дополнительные параметры питания`
4. `Выбор действия по закрытию крышки` или `Выбор действия кнопок питания`
5. Отключите «Сон» для батареи и сети
6. Установите: `При закрытии крышки` → `Ничего не делать`

**Способ 2: Командная строка**
```batch
powercfg /change standby-timeout-ac 0
powercfg /change standby-timeout-dc 0
```

**Способ 3: Периодическое пробуждение**
```batch
REM Компьютер будет пробуждаться каждый час сам (если в BIOS есть опция Wake Timer)
```

---

## 🛠️ ШАГ 7: Решение проблем

### Проблема: Бот не запускается при включении ПК

**Решение:**
1. Откройте `taskschd.msc`
2. Найдите `QAyta Bot`
3. Нажмите右клик → `Запустить`
4. Проверьте статус - должно быть `Готово`

### Проблема: Нет доступа к Google Sheets

**Решение:**
1. Проверьте `config/` папку - должен быть `credentials.json`
2. Если нет - запустите авторизацию вручную:
   ```
   python scripts/data_collection/improved_collector.py
   ```

### Проблема: Бот работает но в Telegram не отвечает

**Решение:**
1. Проверьте `BOT_TOKEN` в скрипте
2. Проверьте интернет соединение
3. Переперезагрузитесь

---

## 📁 Финальная структура

```
C:\QAyta-Bot\
├── scripts/
│   ├── telegram/
│   │   └── working_bot.py
│   ├── data_collection/
│   │   └── improved_collector.py
│   └── analytics/
│       └── analytics_reports.py
├── config/                    # Google Sheets config
├── data/
│   └── fiksa_database.db
├── 123/                       # Загруженные файлы
├── reports/                   # Генерируемые отчеты
├── logs/                      # Логи работы
├── requirements.txt
├── process_period_data.py
├── ОБНОВЛЕНИЕ_КАЖДЫЙ_ЧАС.py   # Ежечасное обновление
└── ЗАПУСК_БОТ.bat             # Автозапуск
```

---

## ✅ Чеклист готовности

- [ ] Скачаны все файлы
- [ ] Установлены зависимости (`pip install -r requirements.txt`)
- [ ] Создан батник `ЗАПУСК_БОТ.bat`
- [ ] Создан скрипт `ОБНОВЛЕНИЕ_КАЖДЫЙ_ЧАС.py`
- [ ] Создана задача в Task Scheduler
- [ ] Отключен режим сна (питание)
- [ ] Бот тестирован в Telegram
- [ ] Проверены логи

---

## 🎉 Готово!

Теперь ваш бот:
- ✅ Запускается автоматически при включении ПК
- ✅ Работает в фоне 24/7
- ✅ Каждый час обновляет данные из Google Sheets
- ✅ Даже если ПК спит, запустится при пробуждении
- ✅ Прерывается только при отключении электричества

**Вопросы?** Контактируйте - помогу настроить!
