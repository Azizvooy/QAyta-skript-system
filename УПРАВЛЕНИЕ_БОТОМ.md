# 🤖 УПРАВЛЕНИЕ TELEGRAM БОТОМ

## 📋 Быстрый старт

### Запуск бота
```batch
start_telegram_bot.bat
```
- Запускает бота в фоновом режиме
- Окно сворачивается автоматически
- Логи пишутся в `logs/telegram_bot.log`
- Можно закрыть окно - бот продолжит работать

### Остановка бота
```batch
stop_telegram_bot.bat
```
- Корректно останавливает бота
- Закрывает все процессы

### Проверка статуса
```batch
check_bot_status.bat
```
- Показывает, работает ли бот
- Выводит PID процесса
- Показывает последние 10 строк лога

### Просмотр логов
```batch
view_bot_logs.bat
```
- Показывает последние 50 строк лога
- Обновляется при каждом запуске

### Перезапуск бота
```batch
restart_telegram_bot.bat
```
- Останавливает и снова запускает бота
- Полезно после обновлений кода

---

## 🔍 Как работает фоновый режим

### Запуск
1. **start_telegram_bot.bat** проверяет, не запущен ли уже бот
2. Активирует виртуальное окружение (если есть `.venv`)
3. Создает папку `logs/` (если нет)
4. Запускает бот в отдельном окне с именем "Telegram Bot FIKSA"
5. Окно автоматически сворачивается (`/MIN`)
6. Бот продолжает работать в фоне

### Работа в фоне
- Бот работает в отдельном процессе Python
- Не блокирует другие скрипты
- Можно запускать сбор данных и аналитику параллельно
- Не мешает работе других программ

### Логирование
- Все события пишутся в файл `logs/telegram_bot.log`
- Формат: `YYYY-MM-DD HH:MM:SS - имя - уровень - сообщение`
- Логи сохраняются даже после перезапуска
- Можно отследить все команды и ошибки

---

## 📊 Что логируется

### Запуск/остановка
```
2026-01-08 10:30:15 - __main__ - INFO - === ЗАПУСК БОТА ===
2026-01-08 10:30:15 - __main__ - INFO - База данных: C:\...\fiksa_database.db
2026-01-08 10:30:15 - __main__ - INFO - Лог-файл: C:\...\telegram_bot.log
2026-01-08 10:30:16 - __main__ - INFO - Приложение создано
2026-01-08 10:30:16 - __main__ - INFO - Обработчики зарегистрированы
2026-01-08 10:30:16 - __main__ - INFO - === БОТ ГОТОВ К РАБОТЕ ===
```

### Команды пользователя
```
2026-01-08 10:35:22 - telegram.ext.Application - INFO - Получена команда: /stats
2026-01-08 10:35:23 - __main__ - INFO - Отправлена статистика пользователю
```

### Ошибки
```
2026-01-08 10:40:10 - __main__ - ERROR - === КРИТИЧЕСКАЯ ОШИБКА === Connection timeout
```

---

## ⚙️ Настройка автозапуска (опционально)

### Windows Task Scheduler

1. Откройте **Планировщик заданий** (Task Scheduler)
2. Создать простую задачу → "Telegram Bot FIKSA"
3. Триггер: **При входе в Windows**
4. Действие: **Запустить программу**
   - Программа: `C:\Users\a.djurayev\Desktop\QAyta skript\start_telegram_bot.bat`
5. Готово!

Теперь бот будет автоматически запускаться при загрузке компьютера.

---

## 🛠️ Решение проблем

### Бот не запускается

**Проверка 1: Уже запущен?**
```batch
check_bot_status.bat
```

**Проверка 2: Есть ли виртуальное окружение?**
```batch
dir .venv\Scripts\python.exe
```

**Проверка 3: Установлены ли зависимости?**
```batch
.venv\Scripts\python.exe -m pip list | findstr telegram
```

### Бот не отвечает

**Проверка 1: Работает ли процесс?**
```batch
check_bot_status.bat
```

**Проверка 2: Есть ли ошибки в логах?**
```batch
view_bot_logs.bat
```

**Решение: Перезапуск**
```batch
restart_telegram_bot.bat
```

### Бот падает с ошибкой

**Проверьте лог:**
```batch
view_bot_logs.bat
```

**Частые причины:**
- Нет доступа к базе данных → проверьте `data/fiksa_database.db`
- Проблемы с интернетом → проверьте подключение
- Неверный токен → проверьте TOKEN в `interactive_bot.py`

---

## 📈 Мониторинг работы

### Проверка каждые 5 минут
Создайте bat-файл `monitor_bot.bat`:
```batch
@echo off
:loop
cls
call check_bot_status.bat
timeout /t 300 /nobreak >nul
goto loop
```

### Автоматический перезапуск при падении
Создайте `keep_alive.bat`:
```batch
@echo off
:loop
call check_bot_status.bat | find "НЕ РАБОТАЕТ" >nul
if %ERRORLEVEL%==0 (
    echo Бот упал! Перезапуск...
    call start_telegram_bot.bat
)
timeout /t 60 /nobreak >nul
goto loop
```

---

## 🎯 Рекомендации

### ✅ Хорошие практики
- Проверяйте статус бота 1-2 раза в день
- Просматривайте логи раз в неделю
- Делайте перезапуск после обновлений кода
- Используйте автозапуск для важных серверов

### ❌ Чего избегать
- Не запускайте несколько копий бота
- Не редактируйте код во время работы бота
- Не удаляйте логи во время работы
- Не завершайте процесс через Task Manager (используйте stop_telegram_bot.bat)

### 💡 Оптимизация
- Очищайте старые логи раз в месяц
- Мониторьте размер лог-файла
- Проверяйте использование памяти (check_bot_status.bat)

---

## 📞 Интеграция с другими процессами

### Параллельная работа

Бот **НЕ БЛОКИРУЕТ** другие процессы:

✅ Можно одновременно:
- Запустить сбор данных (`improved_collector.py`)
- Генерировать отчеты (`analytics_reports.py`)
- Работать с базой данных
- Запускать другие скрипты

### Пример одновременной работы
```batch
REM Окно 1: Бот
start_telegram_bot.bat

REM Окно 2: Сбор данных
python scripts/data_collection/improved_collector.py

REM Окно 3: Аналитика
python scripts/analytics/analytics_reports.py
```

Все процессы работают независимо!

---

## 📝 Структура файлов

```
QAyta skript/
├── start_telegram_bot.bat        ← Запуск бота
├── stop_telegram_bot.bat          ← Остановка бота
├── check_bot_status.bat           ← Проверка статуса
├── view_bot_logs.bat              ← Просмотр логов
├── restart_telegram_bot.bat       ← Перезапуск
│
├── logs/
│   └── telegram_bot.log           ← Лог-файл (создается автоматически)
│
└── scripts/
    └── telegram/
        └── interactive_bot.py     ← Код бота
```

---

## 🎓 Примеры использования

### Утренний запуск
```batch
@echo off
echo Запуск системы FIKSA...
echo.

echo [1/3] Запуск Telegram бота...
call start_telegram_bot.bat

echo [2/3] Сбор данных...
python scripts/data_collection/improved_collector.py

echo [3/3] Генерация отчетов...
python scripts/analytics/analytics_reports.py

echo.
echo Система готова к работе!
pause
```

### Вечерняя остановка
```batch
@echo off
echo Остановка системы...
call stop_telegram_bot.bat
echo Система остановлена
pause
```

---

## 🔄 Changelog

### v2.0 - 2026-01-08
- ✅ Фоновый режим работы
- ✅ Логирование в файл
- ✅ Bat-файлы для управления
- ✅ Проверка уже запущенных процессов
- ✅ Корректная остановка
- ✅ Мониторинг статуса

### v1.0 - 2026-01-07
- Базовая версия бота
- Команды /start, /stats, /operators
- Кнопки для отчетов

---

**Автор:** Система FIKSA  
**Дата:** 8 января 2026  
**Версия:** 2.0
