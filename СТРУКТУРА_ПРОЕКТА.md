# 📁 СТРУКТУРА ПРОЕКТА QAYTA

## Организация по функциям

### 🔄 Основной workflow

```
ПОЛНЫЙ_WORKFLOW.bat          ← ЗАПУСКАЙТЕ ЭТОТ ФАЙЛ для полной обработки
  │
  ├─→ scripts/import/
  │    └─ import_from_sheets.py      (Импорт из Google Sheets → PostgreSQL)
  │
  ├─→ scripts/processing/
  │    └─ process_data.py              (Обработка: создание таблиц в БД)
  │
  └─→ scripts/reports/
       └─ generate_reports.py          (Генерация Excel отчетов)
```

### 📂 Директории проекта

```
c:\Users\a.djurayev\Desktop\QAyta скрипт\
│
├── scripts/                          ← ВСЕ ОСНОВНЫЕ СКРИПТЫ
│   ├── import/
│   │   └── import_from_sheets.py     Импорт данных из Google Sheets
│   │
│   ├── processing/
│   │   └── process_data.py           Обработка данных в PostgreSQL
│   │
│   ├── reports/
│   │   └── generate_reports.py       Генерация Excel отчетов
│   │
│   ├── database/                     ← Существующие скрипты импорта
│   │   ├── import_all_sheets_filtered.py
│   │   └── ...другие...
│   │
│   └── maintenance/                  ← Служебные скрипты
│       └── (резервное копирование, очистка БД и т.д.)
│
├── config/                           ← КОНФИГУРАЦИЯ
│   ├── postgresql.env                БД параметры (хост, пользователь, пароль)
│   ├── google_api.env                Google API ключи
│   └── token.json                    Токен доступа Google
│
├── reports/                          ← СГЕНЕРИРОВАННЫЕ ОТЧЕТЫ
│   └── 2026-02/                      (Месяц-Год)
│       ├── ТОЛЬКО_ОТРИЦАТЕЛЬНЫЕ/     ← Выводятся сюда
│       │   ├── СЛУЖБА_101_ОТРИЦАТЕЛЬНЫЕ.xlsx
│       │   ├── СЛУЖБА_102_ОТРИЦАТЕЛЬНЫЕ.xlsx
│       │   ├── ОБЩИЙ_ОТЧЕТ_ОТРИЦАТЕЛЬНЫЕ.xlsx
│       │   └── СТАТИСТИКА_ОТРИЦАТЕЛЬНЫЕ.xlsx
│       │
│       ├── 2026-01_overall/          (Старые отчеты)
│       └── ...другие месяцы...
│
├── docs/                             ← ДОКУМЕНТАЦИЯ
│   ├── README.md
│   ├── СТРУКТУРА_ПРОЕКТА.md          (этот файл)
│   └── ...другие...
│
├── .venv/                            ← Виртуальное окружение Python
├── requirements.txt                  ← Зависимости Python
│
├── docker-compose.yml                ← Конфигурация Docker (PostgreSQL)
│
└── RUN_FULL_WORKFLOW.py              ← Главный Python скрипт
```

---

## 🔄 Workflow выполнения

### 1️⃣ ИМПОРТ ДАННЫХ
**Файл**: `scripts/import/import_from_sheets.py`
- Подключается к Google Sheets API
- Импортирует данные со всех 35 операторов 112
- Сохраняет в таблицу `fixations` в PostgreSQL
- Логирует результат в `action_history`

**Результат в БД**:
```
TABLE fixations (2.4M+ записей)
  ├── fixation_id (уникальный ID)
  ├── call_date (дата звонка)
  ├── phone (телефон)
  ├── status (статус: "Отрицательный", "Положительно" и т.д.)
  ├── complaint (текст жалобы)
  ├── operator_id → TABLE operators
  ├── region_id → TABLE regions
  └── service_id → TABLE services
```

### 2️⃣ ОБРАБОТКА ДАННЫХ
**Файл**: `scripts/processing/process_data.py`
- Создает таблицы с обработанными данными
- Использует SQL для скорости (14 сек для 2.4М записей)
- Фильтрует по статусу (жалобы, отрицательные, позитивные)

**Результат в БД**:
```
TABLE detailed_reports (2,482,210 записей)
  └── Основной отчет со всеми данными

TABLE negative_complaints (96,420 записей)
  └── Отфильтрованы только отрицательные отзывы
  └── WHERE status LIKE '%жалоб%' OR status LIKE '%отриц%'

TABLE complaints_by_region
  └── Статистика по регионам

TABLE summary_statistics
  └── Общие итоги
```

### 3️⃣ ГЕНЕРАЦИЯ ОТЧЕТОВ
**Файл**: `scripts/reports/generate_reports.py`
- Экспортирует данные в Excel файлы
- Группирует по видам услуг (101, 102, 103, 104...)
- Форматирует с цветными заголовками
- Сохраняет в папку `reports/{МЕСЯЦ_ГОД}/ТОЛЬКО_ОТРИЦАТЕЛЬНЫЕ/`

**Результат в файлах**:
```
reports/2026-02/ТОЛЬКО_ОТРИЦАТЕЛЬНЫЕ/
  ├── СЛУЖБА_101_ОТРИЦАТЕЛЬНЫЕ.xlsx    (7,584 записей)
  ├── СЛУЖБА_102_ОТРИЦАТЕЛЬНЫЕ.xlsx    (8,120 записей)
  ├── СЛУЖБА_103_ОТРИЦАТЕЛЬНЫЕ.xlsx    (6,956 записей)
  ├── СЛУЖБА_104_ОТРИЦАТЕЛЬНЫЕ.xlsx    (5,423 записей)
  ├── ОБЩИЙ_ОТЧЕТ_ОТРИЦАТЕЛЬНЫЕ.xlsx   (96,420 записей всех услуг)
  └── СТАТИСТИКА_ОТРИЦАТЕЛЬНЫЕ.xlsx    (сводная статистика)
```

---

## 🚀 Как запустить

### Простой способ (рекомендуется):

1. **Откройте командную строку** в папке проекта
2. **Запустите файл**:
   ```batch
   ПОЛНЫЙ_WORKFLOW.bat
   ```
3. **Ждите завершения** (обычно 5-10 минут)
4. **Отчеты будут в** `reports/2026-02/ТОЛЬКО_ОТРИЦАТЕЛЬНЫЕ/`

### Продвинутый способ (по этапам):

**Этап 1 - Импорт:**
```bash
python scripts/import/import_from_sheets.py
```

**Этап 2 - Обработка:**
```bash
python scripts/processing/process_data.py
```

**Этап 3 - Генерация отчетов:**
```bash
python scripts/reports/generate_reports.py
```

---

## 📊 Что в БД по-умолчанию

### Исходные таблицы (заполняются при импорте):
- `fixations` - звонки/заявки (2.4М+)
- `operators` - операторы 112 (20 записей)
- `regions` - регионы
- `services` - виды услуг (101, 102, 103, 104...)
- `incidents_112` - классификация инцидентов

### Таблицы обработки (создаются при обработке):
- `detailed_reports` - основной отчет (2.4М+)
- `negative_complaints` - отрицательные (96K+)
- `complaints_by_region` - по регионам
- `summary_statistics` - итоги

---

## 📝 Для каких целей каждый скрипт

| Скрипт | Для чего | Входные данные | Выходные данные |
|--------|----------|---|---|
| `import_from_sheets.py` | Загрузить свежие данные из Google Sheets | Google API OAuth | Таблица `fixations` в БД |
| `process_data.py` | Обработать и отфильтровать данные | Таблица `fixations` | Таблицы: `detailed_reports`, `negative_complaints` |
| `generate_reports.py` | Сделать красивые Excel файлы | Таблица `negative_complaints` | Excel файлы в `reports/` |

---

## 🗂️ Старые файлы (можно удалить)

Следующие файлы больше не используются (заменены на новую структуру):

```
❌ process_and_save_to_db.py              ← заменен на scripts/processing/process_data.py
❌ create_reports_in_sql.py               ← встроен в scripts/processing/process_data.py
❌ ИМПОРТ_И_ОБРАБОТКА_В_БД.bat           ← заменен на ПОЛНЫЙ_WORKFLOW.bat
❌ generate_full_report.py                ← заменен на scripts/reports/generate_reports.py
❌ generate_full_report_OLD.py            ← старая версия
❌ create_service_reports.py              ← функциональность в generate_reports.py
❌ ...и другие дублирующиеся скрипты
```

---

## ⚙️ Конфигурация

### `config/postgresql.env` (ВАЖНО!)
```env
DB_HOST=localhost           # Хост PostgreSQL (если Docker, то localhost)
DB_PORT=5432               # Порт PostgreSQL
DB_NAME=qayta_data         # Имя базы данных
DB_USER=qayta_user         # Пользователь БД
DB_PASSWORD=qayta_password_2026  # ПАРОЛЬ (СТАРЫЙ)
```

### `config/google_api.env` (если требуется импорт)
```env
GOOGLE_API_KEY=...         # API ключ Google Sheets
MASTER_SPREADSHEET_ID=...  # ID Google Sheet с операторами
```

### `docker-compose.yml` (для PostgreSQL)
```yaml
services:
  qayta-postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: qayta_data
      POSTGRES_USER: qayta_user
      POSTGRES_PASSWORD: qayta_password_2026
```

---

## 🐛 Поиск ошибок

### Скрипт зависает при обработке
- Это нормально - обработка 2.4М записей занимает 15-30 секунд
- Смотрите в консоль - должны быть сообщения вида "✓ Создана таблица..."

### "invalid input syntax for type timestamp"
- Проблема с NaT значениями в pandas
- РЕШЕНО: скрипты используют чистый SQL вместо pandas insert

### PostgreSQL не запускается
- Проверьте Docker: `docker ps`
- Перезапустите: `docker-compose up -d`
- Проверьте пароль в БД: `qayta_password_2026`

### Google Sheets API ошибки
- Проверьте токен: `config/token.json` должен существовать
- Удалите токен и переавторизуйтесь
- Используйте прокси если требуется: `http://10.145.62.76:3128`

---

## 📌 Быстрая справка

```bash
# Активировать виртуальное окружение
.venv\Scripts\activate

# Запустить полный workflow
python RUN_FULL_WORKFLOW.py

# Или по этапам
python scripts/import/import_from_sheets.py
python scripts/processing/process_data.py
python scripts/reports/generate_reports.py

# Проверить БД
docker exec qayta-postgres psql -U qayta_user -d qayta_data -c "\dt"

# Очистить БД
docker exec qayta-postgres psql -U qayta_user -d qayta_data -c "DROP TABLE detailed_reports, negative_complaints;"
```

---

**Версия**: 2.0 (2026-02-06)  
**Оптимизировано для**: обработки 2.4М+ записей за 15 сек  
**SQL + Python**: Быстрое создание отчетов  

