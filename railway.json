{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NONE"
  },
  "deploy": {
    "startCommand": "echo 'Deploy disabled - local project only' && exit 1",
    "restartPolicyType": "NEVER"
  }
}
