# 📊 РАБОТА С БАЗОЙ ДАННЫХ

## ✅ Что было сделано

### 1. Создана оптимизированная схема БД

**Файл**: `data/fiksa_database.db` (SQLite)

**Таблицы**:
- `operators` - Операторы (19 записей)
- `services` - Службы (13 записей)
- `regions` - Регионы
- `applications` - Заявки (57,158 записей)
- `fixations` - Фиксации обзвонов (129,521 записей)

### 2. Импортированы все данные

✅ Обработано **103 CSV файла** из `exported_sheets/`
✅ Импортировано **129,521 фиксаций**
✅ Создано **57,158 уникальных заявок**
✅ Зарегистрировано **19 операторов**

### 3. Созданы удобные BAT-файлы

## 🚀 Быстрый старт

### Первый запуск (инициализация)

```bat
Двойной клик: ИНИЦИАЛИЗАЦИЯ_БД.bat
```

Этот файл:
1. Создаст новую БД
2. Предложит импортировать данные
3. Покажет статистику

### Обновление данных

```bat
Двойной клик: ИМПОРТ_ДАННЫХ.bat
```

Импортирует все данные из `exported_sheets/` в БД.

### Проверка статистики

```bat
Двойной клик: ПРОВЕРКА_БД.bat
```

Показывает:
- Количество записей в таблицах
- Топ-10 операторов по количеству фиксаций

## 📁 Структура файлов

```
QAyta-skript-system/
├── data/
│   └── fiksa_database.db          # База данных SQLite
├── exported_sheets/                # CSV файлы для импорта
│   ├── Narziyeva Gavxar.../       # Папки операторов
│   └── ...
├── scripts/
│   └── database/
│       ├── db_schema.py           # Схема БД
│       ├── import_all_data.py     # Импорт данных
│       └── check_db.py            # Проверка статистики
├── logs/
│   └── database/
│       └── import_all_YYYYMMDD.log # Логи импорта
├── ИНИЦИАЛИЗАЦИЯ_БД.bat           # Создание БД
├── ИМПОРТ_ДАННЫХ.bat              # Импорт данных
└── ПРОВЕРКА_БД.bat                # Статистика БД
```

## 🔄 Рабочий процесс

### Ежедневное обновление данных

1. Экспортируйте данные из Google Sheets в `exported_sheets/`
2. Запустите `ИМПОРТ_ДАННЫХ.bat`
3. Проверьте результат через `ПРОВЕРКА_БД.bat`

### Полная переинициализация

Если нужно пересоздать БД с нуля:

1. Запустите `ИНИЦИАЛИЗАЦИЯ_БД.bat`
2. Выберите "Y" для импорта данных
3. Дождитесь завершения

## 📊 Статистика импорта

**Последний импорт**: 31.01.2026

| Таблица | Записей |
|---------|---------|
| Операторы | 19 |
| Службы | 13 |
| Заявки | 57,158 |
| Фиксации | 129,521 |

**Топ-5 операторов**:
1. Narziyeva Gavxar Atxamjanovna: 25,431
2. Xasanova Maftuna Askar qizi: 19,672
3. Sagdullayeva Moxinur Asqar qizi: 16,949
4. Xusniddinova Shaxnoza Akramovna: 16,145
5. Zokirjonova Surayyo Rustam qizi: 14,843

## 💡 Преимущества БД

✅ **Нет дубликатов** - каждая заявка уникальна
✅ **Быстрый поиск** - индексы по ключевым полям
✅ **Связи данных** - операторы, службы, заявки связаны правильно
✅ **История изменений** - логи всех операций
✅ **Нет пустых значений** - валидация при импорте
✅ **Многократное использование** - не нужно пересчитывать каждый раз

## 🔧 Дополнительные возможности

### Создание отчетов

```bat
python scripts/database/db_reports.py
```

### Прямой доступ к БД

```python
import sqlite3
conn = sqlite3.connect('data/fiksa_database.db')
cursor = conn.cursor()

# Пример запроса
cursor.execute("""
    SELECT o.operator_name, COUNT(*) as cnt
    FROM fixations f
    JOIN operators o ON f.operator_id = o.operator_id
    GROUP BY o.operator_id
    ORDER BY cnt DESC
""")

for row in cursor.fetchall():
    print(f'{row[0]}: {row[1]}')
```

### SQL-запросы

База данных поддерживает стандартный SQL:

```sql
-- Количество фиксаций по службам
SELECT s.service_name, COUNT(*) as cnt
FROM applications a
JOIN fixations f ON a.application_id = f.application_id
JOIN services s ON a.service_id = s.service_id
GROUP BY s.service_id
ORDER BY cnt DESC;

-- Фиксации за определенный период
SELECT * FROM fixations
WHERE fixation_date BETWEEN '2025-12-01' AND '2025-12-31';
```

## ❓ Частые вопросы

**Q: Можно ли импортировать данные несколько раз?**
A: Да! Система автоматически обновляет существующие записи и создает новые.

**Q: Что делать, если импорт завис?**
A: Нажмите Ctrl+C и запустите снова. БД сохранит все, что было импортировано до прерывания.

**Q: Как удалить все данные?**
A: Запустите `ИНИЦИАЛИЗАЦИЯ_БД.bat` - это пересоздаст БД с нуля.

**Q: Где хранятся логи?**
A: В папке `logs/database/` - каждый день создается новый файл.

## 📝 Техническая информация

- **СУБД**: SQLite 3
- **Кодировка**: UTF-8
- **Размер БД**: ~50-100 МБ (зависит от количества данных)
- **Скорость импорта**: ~1000 записей/сек
- **Python версия**: 3.13.3
- **Зависимости**: pandas, sqlite3

## 🎯 Следующие шаги

1. ✅ БД создана и заполнена
2. ⏭️ Создать отчеты из БД
3. ⏭️ Интегрировать с Telegram-ботом
4. ⏭️ Добавить автоматическое обновление

---

**Документация обновлена**: 31.01.2026
