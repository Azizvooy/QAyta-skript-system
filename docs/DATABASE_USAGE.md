# СИСТЕМА ЕЖЕДНЕВНОГО СБОРА ДАННЫХ В БД

## 📦 Установлено

Создана система для автоматического сбора данных о фиксации в базу данных SQLite.

## 🗂️ Структура

### База данных: `data/fiksa_database.db`

**Таблица `fiksa_records`** - все записи о фиксации:
- collection_date - дата сбора
- operator_name - имя оператора
- card_number - номер карты
- full_name - ФИО
- phone - телефон
- address - адрес
- status - статус (отрицательный, положительный и т.д.)
- call_date - дата звонка
- notes - примечания

**Таблица `operator_stats`** - статистика по операторам:
- collection_date - дата
- operator_name - оператор
- total_records - всего записей
- status_counts - разбивка по статусам

## 🚀 Как использовать

### 1. Ежедневный сбор данных

```bash
python scripts/data_collection/daily_db_collector.py
```

Этот скрипт:
- Подключается к Google Sheets
- Собирает данные со всех таблиц операторов
- Сохраняет в БД с текущей датой
- Подсчитывает статистику

**Запускайте каждый день!** Можно добавить в планировщик Windows.

### 2. Фильтр и анализ данных

```bash
python scripts/analysis/data_filter.py
```

Интерактивное меню для:
- Статистика за день/период
- Поиск по карте или имени
- Фильтр по оператору
- Фильтр по статусу
- Производительность операторов
- Экспорт в Excel

## 📊 Примеры фильтров

### Из Python кода:

```python
from scripts.analysis.data_filter import *

# Данные за сегодня
df = get_records_by_date('2026-01-07', '2026-01-07')

# Данные оператора
df = get_records_by_operator('Narziyeva Gavxar Atxamjanovna')

# Поиск по карте
df = search_by_card_or_name('1234567')

# Статистика за период
stats = get_period_stats('2026-01-01', '2026-01-07')
print(stats)

# Производительность
df = get_operator_performance('2026-01-01', '2026-01-07')
```

## ⏰ Автоматизация

### Планировщик Windows (каждый день в 18:00):

1. Открыть "Планировщик заданий"
2. Создать простую задачу
3. Триггер: Ежедневно в 18:00
4. Действие: Запустить программу
5. Программа: `python`
6. Аргументы: `"C:\Users\a.djurayev\Desktop\QAyta skript\scripts\data_collection\daily_db_collector.py"`
7. Рабочая папка: `C:\Users\a.djurayev\Desktop\QAyta skript`

## 🔍 SQL запросы напрямую

```sql
-- Всего записей за сегодня
SELECT COUNT(*) FROM fiksa_records WHERE collection_date = '2026-01-07';

-- Топ операторов
SELECT operator_name, COUNT(*) as total 
FROM fiksa_records 
GROUP BY operator_name 
ORDER BY total DESC;

-- По статусам
SELECT status, COUNT(*) as total 
FROM fiksa_records 
WHERE collection_date = '2026-01-07'
GROUP BY status;

-- Динамика по дням
SELECT collection_date, COUNT(*) as total 
FROM fiksa_records 
GROUP BY collection_date 
ORDER BY collection_date;
```

## 📈 Преимущества

✅ История данных хранится навсегда
✅ Быстрый поиск и фильтрация
✅ Анализ трендов и динамики
✅ Сравнение операторов
✅ Экспорт в Excel для отчетов
✅ Не зависит от Google Sheets

## 🛠️ Дополнительные возможности

Можно добавить:
- Автоматические отчеты на email
- Графики и дашборды
- Алерты при аномалиях
- Интеграцию с другими системами
