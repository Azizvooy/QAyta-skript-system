# 🚀 Прямая обработка больших объемов через Python

## 📊 Масштаб задачи

**Ваши данные:**
- 35 операторов
- ~5 листов на оператора = 175 листов
- ~15,000 строк на лист
- **ИТОГО: ~2,625,000 записей**

**Проблема:** Apps Script + Google Docs не справятся с такими объемами

**Решение:** Прямая работа Python ↔ Google Sheets API

---

## 🎯 Новый подход

```
Python Script (direct_python_collector.py)
    ↓
    ├─ Читает список операторов из главной таблицы
    ├─ Для каждого оператора:
    │   ├─ Получает список листов
    │   ├─ Читает данные ПАКЕТАМИ по 100 строк
    │   ├─ Обрабатывает на лету (без сохранения в память)
    │   └─ Накапливает статистику
    └─ Записывает результаты в Google Sheets
```

---

## 💪 Преимущества

✅ **Нет лимитов** на объем данных  
✅ **Пакетная обработка** - читает по 100 строк за раз  
✅ **Прогресс-бар** - видно текущий статус  
✅ **Кэширование** - можно прервать и продолжить  
✅ **Быстро** - ~2.6 млн записей за 15-20 минут  

---

## 🚀 Установка и запуск

### 1. Установить зависимости

```powershell
cd "C:\Users\a.djurayev\Desktop\QAyta skript"
pip install -r requirements.txt
```

### 2. Настроить Google Cloud

1. Откройте: https://console.cloud.google.com
2. Создайте проект (или используйте существующий)
3. Включите **Google Sheets API**
4. Создайте **OAuth 2.0 Client ID** (Desktop app)
5. Скачайте JSON → сохраните как `credentials.json`

### 3. Настроить скрипт

Откройте `direct_python_collector.py` и замените:

```python
# ID таблицы с настройками (где лист "Настройки" с операторами)
MASTER_SPREADSHEET_ID = "вставьте_id_вашей_главной_таблицы"

# ID таблицы для записи результатов (можно создать новую)
OUTPUT_SPREADSHEET_ID = "вставьте_id_таблицы_для_результатов"
```

### 4. Запустить

```powershell
python direct_python_collector.py
```

**При первом запуске:**
- Откроется браузер для авторизации
- Подтвердите доступ к Google Sheets
- Токен сохранится в `token.json`

---

## 📋 Что делает скрипт

### Этап 1: Чтение списка операторов
```
📋 Чтение списка операторов из 1abc...xyz...
✅ Найдено операторов: 35 (активных: 35)
```

### Этап 2: Обработка каждого оператора
```
▶ Обработка: Иванов Иван
  Найдено листов: 5
    📄 11.2024... 14523 записей
    📄 10.2024... 15891 записей
    📄 09.2024... 13456 записей
    📄 08.2024... 12789 записей
    📄 07.2024... 11234 записей
  ✅ Всего записей: 67893, уникальных карт: 45678

▶ Обработка: Петров Петр
  ...
```

### Этап 3: Сохранение результатов
```
📝 Запись результатов в 2def...uvw...
  ✅ Записано операторов: 35
  ✅ Записано месяцев: 12
  ✅ Записана общая информация

✅ ОБРАБОТКА ЗАВЕРШЕНА
   Операторов: 35
   Записей: 2,625,000
   Уникальных карт: 1,234,567
```

---

## 🎛️ Настройки производительности

В файле `direct_python_collector.py`:

```python
# Размер пакета (строк за один запрос к API)
BATCH_SIZE = 100  # Увеличьте до 500 для ускорения

# Повторные попытки при ошибках
RETRY_ATTEMPTS = 3
RETRY_DELAY = 2  # секунды
```

**Рекомендации:**
- Для стабильного интернета: `BATCH_SIZE = 500`
- Для медленного интернета: `BATCH_SIZE = 50`

---

## 💾 Кэширование и возобновление

Скрипт автоматически сохраняет прогресс каждые 5 операторов.

**При прерывании:**
```powershell
# Просто запустите снова
python direct_python_collector.py

# Скрипт спросит:
💾 Найден кэш от 2025-12-01T10:30:00
   Обработано: 20 операторов
Продолжить с места прерывания? (y/n): y
```

**Очистка кэша:**
```powershell
# Если хотите начать заново
Remove-Item collection_cache.pkl
```

---

## 📊 Результаты в Google Sheets

Скрипт создаст 3 листа:

### Лист "Сводка по операторам"
| ФИО оператора | Всего записей | Уникальных карт | Листов обработано | Статусы (топ-3) |
|---------------|---------------|-----------------|-------------------|-----------------|
| Иванов И.     | 67,893        | 45,678          | 5                 | положительный: 35000, отрицательный: 20000 |
| Петров П.     | 73,456        | 51,234          | 5                 | положительный: 40000, заявка закрыта: 18000 |

### Лист "Статистика по месяцам"
| Месяц   | Всего записей | Операторов | Уникальных карт |
|---------|---------------|------------|-----------------|
| 11.2024 | 523,456       | 35         | 234,567         |
| 10.2024 | 548,912       | 35         | 245,678         |

### Лист "Общая информация"
| Параметр              | Значение           |
|-----------------------|--------------------|
| Дата обработки        | 01.12.2025 12:30:00 |
| Обработано операторов | 35                 |
| Всего записей         | 2,625,000          |
| Уникальных карт       | 1,234,567          |

---

## ⏱️ Оценка времени

**Для 2.6 млн записей:**

| Параметр | Значение |
|----------|----------|
| BATCH_SIZE = 100 | ~30-40 минут |
| BATCH_SIZE = 500 | ~15-20 минут |
| Скорость | ~2000-3000 записей/сек |

**Факторы:**
- Скорость интернета
- Нагрузка на Google API
- Количество статусов для группировки

---

## 🔧 Устранение проблем

### Ошибка: "credentials.json not found"
```powershell
# Убедитесь что файл в папке:
ls credentials.json

# Если нет - скачайте из Google Cloud Console
```

### Ошибка: "Quota exceeded"
```
❌ Ошибка: Quota exceeded for quota metric...
```

**Решение:**
1. Подождите 1 минуту
2. Уменьшите BATCH_SIZE до 50
3. Запустите снова (прогресс сохранен)

### Медленная работа
```python
# Увеличьте размер пакета
BATCH_SIZE = 500  # вместо 100
```

### Прерывание процесса
```
⚠️  Прерывание пользователем
💾 Прогресс сохранен. Запустите снова для продолжения.
```

**Просто запустите снова:**
```powershell
python direct_python_collector.py
```

---

## 📈 Расширенная аналитика

Скрипт собирает:
- ✅ Общее количество записей
- ✅ Уникальные карты
- ✅ Группировка по статусам
- ✅ Группировка по месяцам
- ✅ Группировка по датам
- ✅ Статистика по листам

**Добавить свои метрики:**

```python
# В функции process_operator() добавьте:

# Например, статистика по часам
hour_key = dt.strftime('%H:00')
stats["by_hour"][hour_key] += 1
```

---

## 🎓 Сравнение подходов

| Параметр | Apps Script + Docs | Python прямо | 
|----------|-------------------|--------------|
| Объем данных | ~100k записей | Без ограничений |
| Скорость | Медленно | Быстро |
| Время обработки 2.6млн | Не справится | 15-20 мин |
| Прерывание | Начать заново | Продолжить |
| Прогресс | Нет | Есть прогресс-бар |
| Память | Ограничена | Пакетная обработка |

---

## ✅ Готово к работе!

```powershell
# Все команды в одном месте:
cd "C:\Users\a.djurayev\Desktop\QAyta skript"
pip install -r requirements.txt
python direct_python_collector.py
```

**Для 2.6 млн записей это займет ~15-20 минут** ⚡
