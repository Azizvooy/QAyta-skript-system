# 🔧 Настройка WSL для Docker Desktop

## ❗ Проблема

Docker Desktop требует **WSL 2** (Windows Subsystem for Linux) для работы на Windows.

---

## ✅ БЫСТРОЕ РЕШЕНИЕ

### Вариант 1: Автоматическое обновление (Рекомендуется)

**Двойной клик:** `ОБНОВИТЬ_WSL.bat`

Скрипт автоматически:
- Запросит права администратора
- Обновит WSL до последней версии
- Покажет текущую версию WSL

### Вариант 2: Вручную через PowerShell

**Шаг 1:** Открыть PowerShell **от имени администратора**
- Win + X → Windows PowerShell (администратор)

**Шаг 2:** Выполнить команды:

```powershell
# Обновить WSL
wsl --update

# Проверить версию
wsl --version

# Установить WSL 2 как версию по умолчанию
wsl --set-default-version 2
```

---

## 🔄 После обновления WSL

### Перезагрузка (Рекомендуется)

```
Пуск → Перезагрузка
```

После перезагрузки:
1. Запустить Docker Desktop
2. Запустить `DOCKER_АВТОЗАПУСК.bat`

### Без перезагрузки (быстрее, но может не сработать)

1. Закрыть Docker Desktop полностью
2. Перезапустить Docker Desktop
3. Запустить `DOCKER_АВТОЗАПУСК.bat`

---

## 📋 Проверка WSL

Открыть PowerShell и выполнить:

```powershell
# Показать версию WSL
wsl --version

# Показать установленные дистрибутивы
wsl --list --verbose
```

Должно показать:
```
WSL version: 2.x.x или выше
Kernel version: 5.x.x или выше
```

---

## 🚨 Если не работает

### Включить компоненты Windows

1. **Win + R** → `optionalfeatures`
2. Включить:
   - ☑ Подсистема Windows для Linux
   - ☑ Платформа виртуальных машин
3. **Перезагрузить** компьютер
4. Запустить `ОБНОВИТЬ_WSL.bat`

### Установить WSL с нуля

PowerShell от администратора:

```powershell
# Установить WSL 2
wsl --install

# Перезагрузить компьютер после установки!
```

---

## 🐳 Docker Desktop + WSL 2

После успешного обновления WSL:

### Настройки Docker Desktop

1. Открыть Docker Desktop
2. Settings → General
3. Убедиться что включено:
   - ☑ Use WSL 2 based engine

### Проверка работы

```powershell
# Docker должен работать
docker info

# Запустить контейнеры
docker compose up -d
```

---

## ⚡ Полная последовательность действий

```batch
1. ОБНОВИТЬ_WSL.bat                  ← Обновить WSL
2. Перезагрузка компьютера           ← Важно!
3. Запустить Docker Desktop          ← Подождать пока запустится
4. DOCKER_АВТОЗАПУСК.bat             ← Запустить PostgreSQL
5. НАСТРОЙКА_POSTGRESQL.bat          ← Создать схему БД
6. ИМПОРТ_В_POSTGRESQL.bat           ← Импортировать данные
```

**Общее время:** ~15 минут (включая перезагрузку)

---

## 📚 Полезные ссылки

- Официальная документация WSL: https://aka.ms/wslinstall
- Docker Desktop WSL 2: https://docs.docker.com/desktop/wsl/
- Решение проблем WSL: https://learn.microsoft.com/windows/wsl/troubleshooting

---

**Дата:** 5 февраля 2026
