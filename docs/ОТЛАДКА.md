# 🔍 ШПАРГАЛКА ПО ОТЛАДКЕ

## Скрипт не собирает данные? Следуйте этому чек-листу:

### 1️⃣ Проверьте лист "Настройки"

Откройте таблицу-коллектор → лист "Настройки":

```
| A (ФИО)        | B (ID таблицы)              | C (Статус) |
|----------------|----------------------------|------------|
| Иванов Иван    | 1abc...xyz                 | активен    |
| Петров Петр    | 2def...uvw                 | активен    |
```

✅ **Проверьте:**
- [ ] ID таблиц **правильные** (скопированы из URL таблицы оператора)
- [ ] Статус = **"активен"** (маленькими буквами!)
- [ ] Нет пустых строк между операторами

### 2️⃣ Проверьте доступ к таблицам операторов

Для **каждого ID** из листа "Настройки":

1. Откройте URL: `https://docs.google.com/spreadsheets/d/[ID_ТАБЛИЦЫ]/edit`
2. У вас должен быть доступ **"Редактор"** или **"Читатель"**
3. Проверьте что в таблице есть лист **"Статистика"**

### 3️⃣ Проверьте структуру листа "Статистика" в таблице оператора

Откройте таблицу любого оператора → лист "Статистика":

Должна быть примерно такая структура:

```
СТАТИСТИКА ФИКСАЦИИ - НОЯБРЬ 2024
Период: 20.11.2024 - 19.12.2024
Обновлено: 01.12.2024 15:30:00

ОБЩАЯ СТАТИСТИКА
Всего фиксаций:              150
Всего уникальных записей:    120
  ↳ из них ОТКРЫТО:           30
  ↳ из них ЗАКРЫТО:           90

УНИКАЛЬНЫЕ ПО СТАТУСАМ:
  • отрицательный:             45
  • положительный:             25
  • заявка закрыта:            10
  • открыть карту:              5
  • тиббиёт ходими аризаси:     5
```

✅ **Ключевые слова** которые ищет скрипт:
- "период" (в строке с датами)
- "обновлено" (в строке с временем)
- "всего фиксаций:"
- "уникальных записей"
- "↳" и "открыто"/"закрыто"
- "•" и названия статусов

### 4️⃣ Запустите сбор вручную и посмотрите логи

1. Откройте таблицу-коллектор
2. Меню → `📊 Сбор статистики` → `🔄 Обновить все данные`
3. Сразу же откройте **Расширения → Apps Script**
4. Перейдите на вкладку **"Журнал выполнения"** (слева)
5. Смотрите логи в реальном времени

### 5️⃣ Читаем логи

#### ✅ Хорошие логи (всё работает):

```
========================================
НАЧАЛО СБОРА СТАТИСТИКИ
Время: 01.12.2025, 15:30:00
========================================

[1/3] Сбор текущего месяца...
=== СБОР ДАННЫХ ТЕКУЩЕГО МЕСЯЦА ===
Найдено операторов: 5

[1/5] Оператор: Иванов Иван
  ID таблицы: 1abc...xyz
  Статус: активен
  → Подключение к таблице 'Иванов Иван'...
    ✓ Лист найден, строк: 25
    ✅ Собрано: фиксаций=150, уникальных=120, открыто=30, закрыто=90
  ✅ Успешно собрано

[2/5] Оператор: Петров Петр
  ...

ИТОГО: успешно=5, ошибок=0

✅ СБОР ЗАВЕРШЕН ЗА 12 СЕК
```

#### ❌ Плохие логи (проблемы):

**Проблема 1: Лист не найден**
```
[1/5] Оператор: Иванов Иван
  → Подключение к таблице...
    ❌ Лист 'Статистика' НЕ НАЙДЕН
```
**Решение:** В таблице оператора нет листа "Статистика" или он называется по-другому

---

**Проблема 2: Недостаточно данных**
```
    ⚠️ Недостаточно данных (строк: 2)
```
**Решение:** Лист "Статистика" пустой или содержит только заголовки

---

**Проблема 3: Данные не найдены**
```
    ⚠️ Данные не найдены или пустые
    DEBUG: Первые строки листа:
      [1] A=СТАТИСТИКА ФИКСАЦИИ | B=
      [2] A=Период | B=
      [3] A=Обновлено | B=
```
**Решение:** Структура листа не соответствует ожидаемой. Скрипт не нашёл ключевые слова.

---

**Проблема 4: Ошибка доступа**
```
    ❌ ОШИБКА: Exception: You do not have permission...
```
**Решение:** Нет доступа к таблице оператора. Попросите владельца дать вам права "Редактор" или "Читатель"

### 6️⃣ Проверьте триггеры

1. Откройте **Apps Script** таблицы-коллектора
2. Слева нажмите на значок **⏰ (Триггеры)**
3. Должны быть триггеры:

```
collectAllStatistics    - Временной триггер - Каждые 2 часа
sendDailySummaryToTelegram - Временной триггер - Каждый день 13:00
sendDailySummaryToTelegram - Временной триггер - Каждый день 18:00
```

Если триггеров нет:
- Запустите **Меню → ⚙️ Настроить автосбор**
- Подтвердите разрешения

### 7️⃣ Тестовый прогон

Проверим что скрипт может прочитать хотя бы одну таблицу:

1. Откройте **Apps Script** таблицы-коллектора
2. Вставьте этот тестовый код в редактор (временно):

```javascript
function testOneOperator() {
  var operatorId = "ВСТАВЬТЕ_СЮДА_ID_ТАБЛИЦЫ_ОПЕРАТОРА";
  var operatorName = "Тестовый оператор";
  
  Logger.log("Тест чтения таблицы: " + operatorName);
  
  try {
    var ss = SpreadsheetApp.openById(operatorId);
    Logger.log("✓ Таблица открыта");
    
    var sheet = ss.getSheetByName("Статистика");
    if (!sheet) {
      Logger.log("✗ Лист 'Статистика' не найден!");
      return;
    }
    Logger.log("✓ Лист найден");
    
    var lastRow = sheet.getLastRow();
    Logger.log("Строк на листе: " + lastRow);
    
    if (lastRow >= 10) {
      var data = sheet.getRange(1, 1, 10, 2).getValues();
      Logger.log("\nПервые 10 строк:");
      for (var i = 0; i < data.length; i++) {
        Logger.log("[" + (i+1) + "] " + data[i][0] + " | " + data[i][1]);
      }
    }
    
    Logger.log("\n✅ Тест пройден успешно!");
    
  } catch (err) {
    Logger.log("❌ ОШИБКА: " + err);
  }
}
```

3. Замените `"ВСТАВЬТЕ_СЮДА_ID_ТАБЛИЦЫ_ОПЕРАТОРА"` на реальный ID
4. Запустите функцию `testOneOperator`
5. Смотрите логи

### 8️⃣ Частые ошибки

| Ошибка | Причина | Решение |
|--------|---------|---------|
| Статус "неактивен" вместо "активен" | Опечатка в листе "Настройки" | Исправьте статус на "активен" |
| ID таблицы неправильный | Скопирован не полный ID | ID из URL между `/d/` и `/edit` |
| Нет доступа | Владелец не дал права | Попросите права "Читатель" минимум |
| Лист называется по-другому | "Statistics" вместо "Статистика" | Переименуйте лист в "Статистика" |
| Триггеры не создаются | Не подтверждены разрешения | Запустите setupCollectorTriggers повторно |

---

## 📞 Если ничего не помогло

1. Скопируйте **ВСЕ ЛОГИ** из журнала выполнения
2. Сделайте **скриншот листа "Настройки"**
3. Сделайте **скриншот листа "Статистика"** одной из таблиц оператора
4. Отправьте всё это для диагностики

---

## ✅ Контрольный чеклист

Перед тем как просить помощь, убедитесь что:

- [ ] Лист "Настройки" заполнен правильно (ID + статус "активен")
- [ ] У вас есть доступ ко ВСЕМ таблицам операторов
- [ ] В таблицах операторов есть лист "Статистика"
- [ ] Скрипт обновлён до последней версии (из google_sheets_collector.js)
- [ ] Триггеры созданы (⏰ в Apps Script)
- [ ] Вы запускали сбор вручную и смотрели логи
- [ ] Вы прочитали логи и понимаете где именно ошибка

**90% проблем решаются проверкой этих пунктов!** ✨
