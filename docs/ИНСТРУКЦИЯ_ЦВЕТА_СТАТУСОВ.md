# Инструкция по применению цветового форматирования для колонки E (Статусы)

## 📋 Что изменено

Добавлена функция автоматического цветового форматирования для колонки E (Статусы) с следующими цветами:

### Цветовая схема:

1. **Отрицательный** → 🔴 Красный (#ff0000, белый текст)
2. **Положительный** → 🟢 Зеленый (#00ff00, черный текст)
3. **Тишине** → 🩷 Нежно-красный (#ffcccc, черный текст)
4. **Соед прервано** → 🩷 Нежно-красный (#ffcccc, черный текст)
5. **НЕТ ОТВЕТА (ЗАНЯТО)** → 🟡 Желтый (#ffff00, черный текст)
6. **Заявка закрыта (не удалось дозвониться)** → ⚪ Серый (#cccccc, черный текст)
7. **Открыть карту** → 🔵 Небесный (#87ceeb, черный текст)
8. **Тиббиёт ходими аризаси** → 💙 Голубой нежный (#add8e6, черный текст)

## 🚀 Как применить изменения

### Шаг 1: Обновить скрипт в Google Sheets

1. Откройте свою таблицу в Google Sheets
2. Перейдите в **Расширения** → **Apps Script**
3. Найдите файл с кодом (обычно называется `Code.gs`)
4. **ПОЛНОСТЬЮ замените** старый код на новый из файла `google_sheets_complete.js`
5. Нажмите **Ctrl+S** (или кнопку "Сохранить")
6. Закройте редактор Apps Script

### Шаг 2: Применить цветовое форматирование

**Вариант А: Автоматически при настройке**
1. В таблице Google Sheets выберите меню **⚙️ Система Фиксаций** → **⚙️ Настроить систему**
2. Подтвердите разрешения (если требуется)
3. Дождитесь сообщения об успешной настройке
4. Цвета будут применены автоматически

**Вариант Б: Вручную**
1. В таблице Google Sheets выберите меню **⚙️ Система Фиксаций** → **🎨 Применить цвета к статусам**
2. Дождитесь завершения (появится сообщение в правом нижнем углу)
3. Готово!

### Шаг 3: Проверка работы

1. Перейдите на лист **FIKSA**
2. В колонке E выберите любую ячейку
3. Введите один из статусов (например, "положительный")
4. Ячейка должна автоматически окраситься в соответствующий цвет (зеленый)

## 🔧 Устранение неполадок

### Цвета не применяются

1. Убедитесь, что вы обновили код в Apps Script
2. Запустите вручную: **⚙️ Система Фиксаций** → **🎨 Применить цвета к статусам**
3. Проверьте, что названия статусов точно совпадают (с учетом регистра)

### Старые цвета остались

Функция автоматически удаляет старые правила условного форматирования для колонки E и создает новые.

### Цвета применились к другим колонкам

Функция работает **ТОЛЬКО** с колонкой E (статусы). Остальные колонки не затрагиваются.

## 📝 Примечания

- Форматирование применяется ко всем строкам колонки E (до 1000+ строк)
- Цвета сохраняются при копировании и архивировании
- При добавлении новых строк форматирование применяется автоматически
- Можно изменить цвета, отредактировав функцию `applyStatusColorFormatting()` в коде

## 🎨 Как изменить цвета (для продвинутых пользователей)

Если вы хотите изменить цвета:

1. Откройте **Расширения** → **Apps Script**
2. Найдите функцию `applyStatusColorFormatting()`
3. Измените значения в `.setBackground("#XXXXXX")`, где `#XXXXXX` - код цвета
4. Используйте [color-hex.com](https://www.color-hex.com/) для подбора цветов
5. Сохраните и запустите функцию снова

---

**Дата создания:** 07.01.2026  
**Версия скрипта:** 3.1  
**Автор:** GitHub Copilot
