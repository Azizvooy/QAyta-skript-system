# 🤖 РАБОЧИЙ TELEGRAM БОТ - БЫСТРЫЙ СТАРТ

## ⚡ Запуск

### Тестирование (в консоли)
```batch
test_bot.bat
```
Увидите все логи в реальном времени. Ctrl+C для остановки.

### Фоновый режим
```batch
start_telegram_bot.bat
```
Бот работает в фоне, не мешает другим процессам.

---

## 📱 Команды в Telegram

### Основные команды
- `/start` - Главное меню с кнопками
- `/stats` - Статистика БД
- `/help` - Справка

### Обновление данных
- `/update` - Обновить данные из Google Sheets
- `/reports` - Сгенерировать отчеты
- `/full` - **Полное обновление (данные + отчеты)**

### Получение отчетов
- `/operators` - Статистика операторов (Excel)
- `/feedback` - Фидбэки служб (Excel)
- `/service102` - Детальный отчет службы 102
- `/service103` - Детальный отчет службы 103
- `/service104` - Детальный отчет службы 104

---

## 🔄 Типичный процесс работы

### Вариант 1: Кнопки (рекомендуется)
1. Напишите `/start`
2. Нажмите **"🔄 Обновить + Отчеты"**
3. Подождите 2-5 минут
4. Нажмите нужный отчет

### Вариант 2: Команды
1. `/full` - запустит полное обновление
2. Подождите сообщения "✅ Отчеты созданы!"
3. `/operators` - получите отчет по операторам
4. `/feedback` - получите отчет по фидбэкам
5. `/service102` - получите отчет по службе 102

### Вариант 3: Текстом
Просто напишите боту:
- "обнови" → запустит обновление
- "операторы" → отправит отчет
- "статистика" → покажет данные БД
- "102" → отправит отчет службы 102

---

## 📊 Что делает бот

### 🔄 Обновление данных (`/update`)
1. Запускает `improved_collector.py`
2. Собирает данные из всех листов Google Sheets
3. Фильтрует по столбцу E (только заполненные)
4. Загружает в БД `fiksa_database.db`
5. Показывает количество собранных записей

**Время:** 1-3 минуты (зависит от объема данных)

### 📋 Генерация отчетов (`/reports`)
1. Запускает `analytics_reports.py`
2. Создает 3 аналитических отчета:
   - `operator_stats.xlsx` - статистика операторов
   - `service_feedback.xlsx` - фидбэки служб (3 листа)
   - `citizen_responses.xlsx` - типы ответов граждан
3. Создает 61 детальный отчет по службам:
   - `service_102_detailed.xlsx` (18+ операторов)
   - `service_103_detailed.xlsx` (22+ операторов)
   - `service_104_detailed.xlsx` (21+ оператор)

**Время:** 1-2 минуты

### 🚀 Полное обновление (`/full`)
Выполняет `/update` + `/reports` последовательно.

**Время:** 3-5 минут

---

## 📁 Где сохраняются отчеты

```
reports/
├── analytics/
│   ├── operator_stats.xlsx        ← /operators
│   ├── service_feedback.xlsx      ← /feedback
│   └── citizen_responses.xlsx
│
└── services/
    ├── service_102_detailed.xlsx  ← /service102
    ├── service_103_detailed.xlsx  ← /service103
    └── service_104_detailed.xlsx  ← /service104
```

---

## 🔍 Проверка работы

### Статус бота
```batch
check_bot_status.bat
```

### Логи
```batch
view_bot_logs.bat
```

Или смотрите файл: `logs/telegram_bot.log`

---

## 🎯 Примеры использования

### Утренний процесс (9:00)
```
Вы → /full
Бот → 🚀 Запускаю полное обновление...
Бот → 🔄 Начинаю обновление данных из FIKSA...
Бот → ✅ Данные обновлены! 📊 Собрано записей: 1176
Бот → 📊 Генерирую отчеты...
Бот → ✅ Отчеты созданы!
Бот → 📊 СТАТИСТИКА БД
      📁 FIKSA записи: 13,070
      📞 Звонки 112: 25,372
      ...

Вы → /operators
Бот → 📊 Готовлю отчет по операторам...
Бот → [отправляет файл Операторы_Статистика.xlsx]
```

### Быстрая проверка
```
Вы → статистика
Бот → 📊 СТАТИСТИКА БД
      📁 FIKSA записи: 13,070
      📞 Звонки 112: 25,372
      📋 Заявки: 23,150
      📅 Сегодня добавлено: 15
      🕐 Последнее обновление: 2026-01-08 09:23:45
```

### Срочный отчет по службе
```
Вы → 102
Бот → 📞 Готовлю отчет по службе 102...
Бот → [отправляет файл Служба_102_Детально.xlsx]
```

---

## ⚠️ Возможные ошибки

### "❌ Скрипт сбора не найден"
**Причина:** Нет файла `improved_collector.py`  
**Решение:** Проверьте путь `scripts/data_collection/improved_collector.py`

### "❌ Файл не найден. Попробуйте /update"
**Причина:** Отчеты не сгенерированы  
**Решение:** Выполните `/full` или `/reports`

### "⏱ Превышено время ожидания"
**Причина:** Сбор данных занял >10 минут  
**Решение:** Проверьте интернет, попробуйте еще раз

### "❌ Ошибка обновления"
**Причина:** Проблема с Google Sheets API или credentials.json  
**Решение:** 
1. Проверьте `credentials.json`
2. Проверьте `token.json`
3. Запустите вручную: `python scripts/data_collection/improved_collector.py`

---

## 🛠️ Техническая информация

### Что происходит при `/update`
```python
subprocess.run([
    sys.executable, 
    'scripts/data_collection/improved_collector.py'
])
```
1. Подключается к Google Sheets API
2. Читает все листы операторов
3. Фильтрует записи (столбец E должен быть заполнен)
4. Сохраняет в SQLite: `data/fiksa_database.db`
5. Таблицы: `fiksa_records`, `call_history_112`, `applications`

### Что происходит при `/reports`
```python
subprocess.run([
    sys.executable,
    'scripts/analytics/analytics_reports.py'
])
```
1. Читает данные из БД
2. Группирует по операторам
3. Анализирует фидбэки
4. Создает Excel файлы
5. Сохраняет в `reports/analytics/` и `reports/services/`

### Логирование
Все действия записываются в `logs/telegram_bot.log`:
```
2026-01-08 10:15:23 - __main__ - INFO - === ЗАПУСК БОТА ===
2026-01-08 10:20:45 - __main__ - INFO - Начало обновления данных FIKSA
2026-01-08 10:22:18 - __main__ - INFO - ✅ Данные обновлены!
2026-01-08 10:22:20 - __main__ - INFO - Начало генерации отчетов
2026-01-08 10:23:45 - __main__ - INFO - ✅ Отчеты созданы!
```

---

## 🎮 Управление

### Запуск
```batch
start_telegram_bot.bat     ← Фоновый режим
test_bot.bat               ← Тестирование (видны логи)
```

### Остановка
```batch
stop_telegram_bot.bat      ← Корректная остановка
```

### Мониторинг
```batch
check_bot_status.bat       ← Статус + последние 10 строк лога
view_bot_logs.bat          ← Последние 50 строк лога
```

### Перезапуск
```batch
restart_telegram_bot.bat   ← Остановка + Запуск
```

---

## 📞 Конфигурация

### Telegram
```python
TOKEN = '8141079204:AAErNrjLqTu4Vj1_7VS2kjGFKcR3lU9L9N4'
CHAT_ID = 2012682567
```

### Пути
```python
DB_PATH = 'data/fiksa_database.db'
REPORTS_DIR = 'reports/'
ANALYTICS_DIR = 'reports/analytics/'
SERVICES_DIR = 'reports/services/'
LOG_FILE = 'logs/telegram_bot.log'
```

### Таймауты
```python
timeout=600  # 10 минут на сбор данных
timeout=600  # 10 минут на генерацию отчетов
```

---

## ✅ Проверочный список

Перед началом работы убедитесь:

- [x] Установлен Python 3.11
- [x] Установлены библиотеки (`pip install -r requirements.txt`)
- [x] Есть `credentials.json` (Google API)
- [x] Есть `token.json` (авторизация)
- [x] Создана БД `data/fiksa_database.db`
- [x] Telegram токен правильный
- [x] Chat ID правильный

### Тест связи
```batch
test_bot.bat
```

Должно появиться:
```
🤖 TELEGRAM БОТ FIKSA
====================================
Запуск: 2026-01-08 10:30:15
✅ БОТ РАБОТАЕТ
```

Напишите боту `/start` - должно прийти меню с кнопками.

---

## 🚀 Готово!

Бот полностью настроен и готов к работе:

✅ Собирает данные из Google Sheets  
✅ Обновляет базу данных  
✅ Генерирует отчеты автоматически  
✅ Отправляет отчеты по запросу  
✅ Работает в фоне 24/7  
✅ Логирует все действия  

**Напишите боту `/start` и начинайте работать!**
