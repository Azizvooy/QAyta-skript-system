🚀 QAYTA СИСТЕМА ОБРАБОТКИ ДАННЫХ 2.0

# Быстрый старт

## 1. Запуск в 1 клик

### Вариант A (Рекомендуется)
```batch
ПОЛНЫЙ_WORKFLOW.bat
```

### Вариант B (Python)
```bash
python RUN_FULL_WORKFLOW.py
```

---

## 📊 Что происходит за кулисами

```
1️⃣  ИМПОРТ (5-10 минут)
    Google Sheets (35 операторов) → PostgreSQL (fixations таблица)
    2,482,210 записей загружается

2️⃣  ОБРАБОТКА (14.8 секунд)
    → detailed_reports (все данные)
    → negative_complaints (96,420 отрицательных)
    → complaints_by_region (по регионам)
    → summary_statistics (итоги)

3️⃣  ОТЧЕТЫ (30 секунд)
    → СЛУЖБА_101_ОТРИЦАТЕЛЬНЫЕ.xlsx
    → СЛУЖБА_102_ОТРИЦАТЕЛЬНЫЕ.xlsx
    → ... и другие по услугам
    → ОБЩИЙ_ОТЧЕТ_ОТРИЦАТЕЛЬНЫЕ.xlsx
    → СТАТИСТИКА_ОТРИЦАТЕЛЬНЫЕ.xlsx
```

**Все файлы сохраняются в**: `reports/2026-02/ТОЛЬКО_ОТРИЦАТЕЛЬНЫЕ/`

---

## 📁 Структура проекта

```
📦 QAyta скрипт
 ├── RUN_FULL_WORKFLOW.py              ← ГЛАВНЫЙ СКРИПТ
 ├── ПОЛНЫЙ_WORKFLOW.bat               ← BATCH ФАЙЛ ДЛЯ ЗАПУСКА
 │
 ├── scripts/
 │  ├── import/
 │  │  └── import_from_sheets.py       (Импорт из Google Sheets)
 │  ├── processing/
 │  │  └── process_data.py             (Обработка в БД - 14 сек)
 │  ├── reports/
 │  │  └── generate_reports.py         (Excel отчеты)
 │  └── maintenance/                   (Служебные скрипты)
 │
 ├── config/
 │  ├── postgresql.env                 (БД параметры)
 │  └── google_api.env                 (Google API)
 │
 ├── reports/
 │  └── 2026-02/
 │     └── ТОЛЬКО_ОТРИЦАТЕЛЬНЫЕ/       ← РЕЗУЛЬТАТЫ ЗДЕСЬ
 │
 └── СТРУКТУРА_ПРОЕКТА.md              (Полная документация)
```

---

## ✨ Основные особенности

### 1. Быстро
- Обработка 2.4M записей за 14.8 секунд (было зависает)
- Использование SQL вместо pandas
- Все таблицы создаются одной транзакцией

### 2. Организованно
- Было: 100+ разных файлов в корне
- Стало: 4 логичные папки по функциям
- Каждый скрипт с документацией

### 3. Просто
- Одна команда для запуска всего
- Автоматическое создание папок
- Excel файлы уже отформатированы

---

## 🔧 Требования

✅ Python 3.10+  
✅ PostgreSQL 16 (можно через Docker)  
✅ Google Sheets API доступ  
✅ Интернет (для синхронизации)  

### Проверить параметры БД:

```bash
# Посмотреть конфигурацию
cat config/postgresql.env

# Проверить подключение к БД
docker exec qayta-postgres psql -U qayta_user -d qayta_data -c "\dt"
```

---

## 📊 Результаты

После выполнения workflow вы получите:

### 5 таблиц в PostgreSQL:
| Таблица | Записей | Назначение |
|---------|---------|-----------|
| `detailed_reports` | 2,482,210 | Основной отчет |
| `negative_complaints` | 96,420 | Только отрицательные |
| `complaints_by_region` | 1-X | По регионам |
| `summary_statistics` | 1 | Итоги |
| `fixations` | 2,482,210 | Исходные (импорт) |

### 6+ Excel файлов:
```
СЛУЖБА_101_ОТРИЦАТЕЛЬНЫЕ.xlsx
СЛУЖБА_102_ОТРИЦАТЕЛЬНЫЕ.xlsx
СЛУЖБА_103_ОТРИЦАТЕЛЬНЫЕ.xlsx
СЛУЖБА_104_ОТРИЦАТЕЛЬНЫЕ.xlsx
ОБЩИЙ_ОТЧЕТ_ОТРИЦАТЕЛЬНЫЕ.xlsx
СТАТИСТИКА_ОТРИЦАТЕЛЬНЫЕ.xlsx
```

---

## ❓ Часто задаваемые вопросы

### Q: Как долго это выполняется?
**A**: 15-20 минут (импорт зависит от интернета)

### Q: Что если я хочу только отчеты (БД уже заполнена)?
```bash
python scripts/reports/generate_reports.py
```

### Q: Где найти созданные Excel файлы?
```
reports/2026-02/ТОЛЬКО_ОТРИЦАТЕЛЬНЫЕ/
```

### Q: Как переобработать только данные в БД?
```bash
python scripts/processing/process_data.py
```

### Q: Видеть полную логику?
Откройте `СТРУКТУРА_ПРОЕКТА.md`

---

## 🐛 Решение проблем

### PostgreSQL не запускается
```bash
docker-compose up -d
docker exec qayta-postgres psql -U qayta_user -d qayta_data
```

### Google API ошибка
1. Удалите `config/token.json`
2. Переавторизуйтесь (система запросит)
3. Удалите прокси или настройте его

### Excel файлы не создаются
Проверьте права доступа в папке `reports/`

### Скрипт зависает
Это нормально для импорта (3-5 мин). Обработка БД занимает 14 сек.

---

## 📞 Поддержка

Полная документация: **СТРУКТУРА_ПРОЕКТА.md**
Отчет о разработке: **ИТОГОВЫЙ_ОТЧЕТ.md**

---

## 🎉 Готово!

Просто запустите:
```batch
ПОЛНЫЙ_WORKFLOW.bat
```

И наслаждайтесь результатами в `reports/2026-02/ТОЛЬКО_ОТРИЦАТЕЛЬНЫЕ/`

---

**Версия**: 2.0  
**Обновлено**: 6 февраля 2026  
**Статус**: ✅ Производство
