# ✅ ИСКЛЮЧЕНИЕ ЛИШНИХ СТРОК ИЗ ОТЧЕТОВ

**Дата:** 08.01.2026  
**Статус:** Реализовано автоматическое исключение

---

## 🔴 ЧТО ИСКЛЮЧАЕТСЯ

Из отчетов **автоматически удаляются** строки, содержащие:

### Паттерны исключения:
- ❌ **Тренды**
- ❌ **Текущий месяц - Сводка**
- ❌ **Предыдущий месяц - Сводка**
- ❌ **СВОДКА СОТРУДНИКИ**
- ❌ **Ноябрь 2025**
- ❌ **Декабрь 2025**
- ❌ Любые строки со словом **"сводка"**
- ❌ Любые строки со словом **"итого"**

---

## 🛠️ ЧТО БЫЛО СДЕЛАНО

### 1. Обновлены скрипты генерации отчетов

#### `scripts/analysis/analytics_reports.py`
Добавлена фильтрация в функции:
- `operator_performance_report()` - статистика операторов
- `citizen_response_analysis()` - ответы граждан

**Как работает:**
```python
# Фильтруем исключаемые строки
if not df.empty and 'Оператор' in df.columns:
    for pattern in EXCLUDE_PATTERNS:
        df = df[~df['Оператор'].str.contains(pattern, case=False, na=False)]
```

### 2. Обновлен импорт данных

#### `scripts/database/db_import.py`
Добавлена функция `should_exclude_operator()`:
- Проверяет имя оператора на соответствие паттернам
- Автоматически пропускает при импорте
- Не создает записи в таблице `operators`

**Результат:** Лишние данные **не попадут в БД** при импорте.

### 3. Создан скрипт очистки существующих данных

#### `scripts/database/clean_db.py`
- Находит всех операторов по паттернам
- Показывает список для подтверждения
- Удаляет операторов и все связанные данные:
  - Фиксации (fixations)
  - Статистика (daily_statistics)

**Запуск:**
```batch
ОЧИСТИТЬ_БД.bat
```

или

```bash
python scripts/database/clean_db.py
```

---

## 📊 ГДЕ ПРИМЕНЯЕТСЯ

### Автоматическая фильтрация в:

1. **Статистика операторов**
   - Файл: `СТАТИСТИКА_ОПЕРАТОРОВ_{дата}.xlsx`
   - Скрипт: `analytics_reports.py`

2. **Ответы граждан**
   - Файл: `ОТВЕТЫ_ГРАЖДАН_{дата}.xlsx`
   - Скрипт: `analytics_reports.py`

3. **Импорт данных**
   - При загрузке из Excel/CSV
   - При импорте из Google Sheets

---

## 🚀 КАК ИСПОЛЬЗОВАТЬ

### Вариант 1: Очистить существующую БД

```batch
ОЧИСТИТЬ_БД.bat
```

Скрипт:
1. Найдет всех операторов по паттернам
2. Покажет список
3. Попросит подтверждение
4. Удалит данные

### Вариант 2: Новый импорт (автоматически)

Просто импортируйте данные:
```batch
УПРАВЛЕНИЕ_БД.bat
→ [2] Импортировать данные
```

Лишние строки **автоматически пропустятся**.

### Вариант 3: Генерация отчетов (автоматически)

Генерируйте отчеты как обычно:
```batch
python scripts/analysis/analytics_reports.py
```

Лишние строки **автоматически отфильтруются**.

---

## 📝 ДОБАВЛЕНИЕ НОВЫХ ПАТТЕРНОВ

Если нужно исключить новые строки:

### 1. Отметьте красным в Excel примере
- Откройте файл в `ПРИМЕРЫ_ОТЧЕТОВ/`
- Выделите строку красным фоном
- Сохраните и отправьте

### 2. Или добавьте в код вручную

**Файл:** `scripts/analysis/analytics_reports.py`

Найдите:
```python
EXCLUDE_PATTERNS = [
    'Тренды',
    'Текущий месяц - Сводка',
    # ...
]
```

Добавьте свой паттерн:
```python
EXCLUDE_PATTERNS = [
    'Тренды',
    'Текущий месяц - Сводка',
    'Ваш новый паттерн',  # ← добавить
]
```

**Файл:** `scripts/database/db_import.py`

То же самое - добавить в список `EXCLUDE_PATTERNS`.

---

## ✅ ПРОВЕРКА РАБОТЫ

### Проверить отчет:
```batch
python scripts/analysis/analytics_reports.py
```

Откройте `СТАТИСТИКА_ОПЕРАТОРОВ_{дата}.xlsx` - не должно быть:
- ❌ Тренды
- ❌ Сводок
- ❌ Итогов

### Проверить БД:
```sql
SELECT operator_name FROM operators 
WHERE LOWER(operator_name) LIKE '%сводка%' 
   OR LOWER(operator_name) LIKE '%итого%'
   OR LOWER(operator_name) LIKE '%тренды%';
```

Должно вернуть: **0 строк**.

---

## 📂 ФАЙЛЫ ПРОЕКТА

### Созданные/измененные:
- ✅ `scripts/analysis/analytics_reports.py` - фильтрация в отчетах
- ✅ `scripts/database/db_import.py` - фильтрация при импорте
- ✅ `scripts/database/clean_db.py` - очистка существующей БД
- ✅ `ОЧИСТИТЬ_БД.bat` - запуск очистки
- ✅ `СТРОКИ_ДЛЯ_УДАЛЕНИЯ.txt` - список паттернов

### Логи:
- `logs/database/cleanup_{дата}.log` - логи очистки
- `logs/database/import_log_{дата}.log` - логи импорта

---

## 🎯 РЕЗУЛЬТАТ

### Было (с лишними строками):
```
Оператор                          | Всего звонков
----------------------------------|---------------
Иванов Иван                       | 150
Петров Петр                       | 120
Тренды                            | 358        ← ЛИШНЕЕ
Текущий месяц - Сводка            | 35         ← ЛИШНЕЕ
```

### Стало (чистые данные):
```
Оператор                          | Всего звонков
----------------------------------|---------------
Иванов Иван                       | 150
Петров Петр                       | 120
```

---

## ⚠️ ВАЖНО

1. **Перед очисткой БД** - сделайте резервную копию:
   ```batch
   copy data\fiksa_database.db data\fiksa_database_backup.db
   ```

2. **После очистки** - заново сгенерируйте отчеты

3. **При импорте новых данных** - фильтрация произойдет автоматически

---

**Система настроена! Лишние строки больше не будут попадать в отчеты и БД.** ✅
